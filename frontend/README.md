# SpreadsheetFilterApp Frontend

## Run

```bash
npm install
npm run dev
```

## Notes

- Expects backend endpoints under `/api/spreadsheets/*`.
- Uses Monaco editor with LINQ snippets and column autocomplete.
- Supports upload (CSV/XLSX), validate, run, result preview, and download.
